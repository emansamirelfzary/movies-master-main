import { RemoveNullTvPipe } from './remove-null-tv.pipe';

describe('RemoveNullTvPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNullTvPipe();
    expect(pipe).toBeTruthy();
  });
});
