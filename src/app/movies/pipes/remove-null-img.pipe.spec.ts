import { RemoveNullImgPipe } from './remove-null-img.pipe';

describe('RemoveNullImgPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNullImgPipe();
    expect(pipe).toBeTruthy();
  });
});
